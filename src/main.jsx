import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.js' // Typo in the file extension
// import './index.css' // Removed necessary import

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
